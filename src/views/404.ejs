<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page Not Found - NutriScan.AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #a3e635;
            --primary-dark: #84cc16;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: #0f172a;
            color: white;
        }

        .dashboard-gradient {
            background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
        }

        .card-shadow {
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.3);
        }

        .floating {
            animation: floating 3s ease-in-out infinite;
        }

        @keyframes floating {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
            100% { transform: translateY(0px); }
        }

        .pulse-glow {
            animation: pulse-glow 2s ease-in-out infinite alternate;
        }

        @keyframes pulse-glow {
            from { box-shadow: 0 0 20px rgba(163, 230, 53, 0.3); }
            to { box-shadow: 0 0 30px rgba(163, 230, 53, 0.6); }
        }
    </style>
</head>
<body class="overflow-x-hidden bg-slate-900 text-white font-inter scroll-smooth">
<div class="min-h-screen flex flex-col">
    <!-- Header -->
    <header class="bg-slate-800 p-4 flex items-center justify-between">
        <div class="flex items-center space-x-4">
            <div class="flex items-center space-x-3">
                <div class="w-10 h-10 rounded-full bg-gradient-to-br from-lime-400 via-green-500 to-emerald-600 flex items-center justify-center relative">
                    <div class="w-6 h-6 rounded-full bg-white flex items-center justify-center">
                        <div class="w-3 h-3 rounded-full bg-gradient-to-br from-orange-400 to-red-500"></div>
                    </div>
                </div>
                <span class="text-xl font-bold">NutriScan.AI</span>
            </div>
        </div>
        <div class="flex items-center space-x-4">
            <a href="/dashboard" class="text-slate-300 hover:text-white transition-colors">
                <i class="fas fa-home mr-1"></i> Dashboard
            </a>
        </div>
    </header>

    <!-- Main Content -->
    <main class="flex-1 flex items-center justify-center p-6">
        <div class="w-full max-w-4xl text-center">
            <!-- Animated 404 Illustration -->
            <div class="relative mb-8">
                <div class="floating mx-auto w-64 h-64 bg-gradient-to-br from-slate-800 to-slate-700 rounded-full flex items-center justify-center card-shadow">
                    <div class="text-center">
                        <div class="text-8xl font-bold text-lime-500 mb-2">404</div>
                        <div class="w-16 h-1 bg-lime-500 mx-auto mb-4"></div>
                        <div class="text-slate-400 text-lg">Page Not Found</div>
                    </div>
                </div>

                <!-- Floating elements around the main circle -->
                <div class="absolute top-10 left-1/4 w-8 h-8 bg-red-500/20 rounded-full flex items-center justify-center floating" style="animation-delay: 0.5s;">
                    <i class="fas fa-utensils text-red-500 text-xs"></i>
                </div>
                <div class="absolute top-20 right-1/4 w-10 h-10 bg-yellow-500/20 rounded-full flex items-center justify-center floating" style="animation-delay: 1s;">
                    <i class="fas fa-apple-alt text-yellow-500"></i>
                </div>
                <div class="absolute bottom-20 left-1/3 w-12 h-12 bg-blue-500/20 rounded-full flex items-center justify-center floating" style="animation-delay: 1.5s;">
                    <i class="fas fa-camera text-blue-500"></i>
                </div>
                <div class="absolute bottom-10 right-1/3 w-9 h-9 bg-green-500/20 rounded-full flex items-center justify-center floating" style="animation-delay: 2s;">
                    <i class="fas fa-weight-scale text-green-500 text-sm"></i>
                </div>
            </div>

            <!-- Message Section -->
            <div class="dashboard-gradient rounded-2xl p-8 mb-8 card-shadow">
                <h1 class="text-3xl font-bold mb-4">Oops! We've Lost This Page</h1>
                <p class="text-slate-300 max-w-2xl mx-auto mb-6">
                    It seems the page you're looking for has been misplaced or doesn't exist.
                    Don't worry, even the best nutrition plans sometimes need adjustment!
                </p>

                <!-- Search Bar -->
                <div class="max-w-md mx-auto mb-8">
                    <div class="relative">
                        <input type="text" placeholder="Search for meals, nutrition tips..."
                               class="w-full bg-slate-700 border border-slate-600 rounded-lg px-4 py-3 text-white focus:outline-none focus:ring-2 focus:ring-lime-500 pl-12">
                        <i class="fas fa-search absolute left-4 top-1/2 transform -translate-y-1/2 text-slate-400"></i>
                    </div>
                </div>

                <!-- Quick Actions -->
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-8">
                    <div class="bg-slate-800/50 rounded-xl p-4 hover:bg-slate-700/50 transition-colors cursor-pointer">
                        <div class="w-12 h-12 bg-lime-500/20 rounded-full flex items-center justify-center mx-auto mb-3">
                            <i class="fas fa-home text-lime-500"></i>
                        </div>
                        <h3 class="font-semibold mb-1">Go to Dashboard</h3>
                        <p class="text-slate-400 text-sm">Return to your main dashboard</p>
                    </div>
                    <div class="bg-slate-800/50 rounded-xl p-4 hover:bg-slate-700/50 transition-colors cursor-pointer">
                        <div class="w-12 h-12 bg-blue-500/20 rounded-full flex items-center justify-center mx-auto mb-3">
                            <i class="fas fa-camera text-blue-500"></i>
                        </div>
                        <h3 class="font-semibold mb-1">Scan a Meal</h3>
                        <p class="text-slate-400 text-sm">Analyze your food with AI</p>
                    </div>
                    <div class="bg-slate-800/50 rounded-xl p-4 hover:bg-slate-700/50 transition-colors cursor-pointer">
                        <div class="w-12 h-12 bg-purple-500/20 rounded-full flex items-center justify-center mx-auto mb-3">
                            <i class="fas fa-utensils text-purple-500"></i>
                        </div>
                        <h3 class="font-semibold mb-1">Meal Plans</h3>
                        <p class="text-slate-400 text-sm">Browse nutrition plans</p>
                    </div>
                </div>

                <!-- Action Buttons -->
                <div class="flex flex-col sm:flex-row justify-center gap-4">
                    <a href="/dashboard"
                       class="bg-lime-500 text-slate-900 px-6 py-3 rounded-lg font-bold hover:bg-lime-400 transition-colors flex items-center justify-center space-x-2 pulse-glow">
                        <i class="fas fa-home"></i>
                        <span>Back to Dashboard</span>
                    </a>
                    <a href="/ai-analysis"
                       class="bg-slate-700 text-white px-6 py-3 rounded-lg font-bold hover:bg-slate-600 transition-colors flex items-center justify-center space-x-2">
                        <i class="fas fa-camera"></i>
                        <span>Scan Meal</span>
                    </a>
                    <button onclick="history.back()"
                            class="border border-slate-600 text-slate-300 px-6 py-3 rounded-lg font-bold hover:bg-slate-800 transition-colors flex items-center justify-center space-x-2">
                        <i class="fas fa-arrow-left"></i>
                        <span>Go Back</span>
                    </button>
                </div>
            </div>

            <!-- Help Section -->
            <div class="bg-slate-800 rounded-xl p-6 card-shadow">
                <h3 class="text-xl font-bold mb-4 flex items-center justify-center">
                    <i class="fas fa-life-ring text-lime-500 mr-2"></i>
                    Need Help?
                </h3>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6 text-left">
                    <div>
                        <h4 class="font-semibold text-lime-400 mb-2">Common Solutions</h4>
                        <ul class="text-slate-400 space-y-2">
                            <li class="flex items-center space-x-2">
                                <i class="fas fa-check text-green-500 text-sm"></i>
                                <span>Check the URL for typos</span>
                            </li>
                            <li class="flex items-center space-x-2">
                                <i class="fas fa-check text-green-500 text-sm"></i>
                                <span>Use the search bar above</span>
                            </li>
                            <li class="flex items-center space-x-2">
                                <i class="fas fa-check text-green-500 text-sm"></i>
                                <span>Navigate from the dashboard</span>
                            </li>
                        </ul>
                    </div>
                    <div>
                        <h4 class="font-semibold text-lime-400 mb-2">Get Support</h4>
                        <ul class="text-slate-400 space-y-2">
                            <li class="flex items-center space-x-2">
                                <i class="fas fa-envelope text-blue-500 text-sm"></i>
                                <span>support@nutriscan.ai</span>
                            </li>
                            <li class="flex items-center space-x-2">
                                <i class="fas fa-comments text-green-500 text-sm"></i>
                                <span>Live Chat Support</span>
                            </li>
                            <li class="flex items-center space-x-2">
                                <i class="fas fa-book text-purple-500 text-sm"></i>
                                <span>Help Documentation</span>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-slate-800 p-6 mt-8">
        <div class="max-w-6xl mx-auto text-center">
            <div class="flex items-center justify-center space-x-3 mb-4">
                <div class="w-8 h-8 rounded-full bg-gradient-to-br from-lime-400 via-green-500 to-emerald-600 flex items-center justify-center">
                    <div class="w-4 h-4 rounded-full bg-white flex items-center justify-center">
                        <div class="w-2 h-2 rounded-full bg-gradient-to-br from-orange-400 to-red-500"></div>
                    </div>
                </div>
                <span class="text-lg font-bold">NutriScan.AI</span>
            </div>
            <p class="text-slate-400 text-sm">
                &copy; 2024 NutriScan.AI - AI-Powered Nutrition Analysis.
                This page serves healthy 404 errors with zero calories.
            </p>
        </div>
    </footer>
</div>

<script>
    // Add some interactive elements
    document.addEventListener('DOMContentLoaded', function() {
        // Add click handlers to quick action cards
        const actionCards = document.querySelectorAll('.bg-slate-800\\/50');
        actionCards.forEach(card => {
            card.addEventListener('click', function() {
                const text = this.querySelector('h3').textContent;
                if (text.includes('Dashboard')) {
                    window.location.href = '/dashboard';
                } else if (text.includes('Scan')) {
                    window.location.href = '/ai-analysis';
                } else if (text.includes('Meal Plans')) {
                    window.location.href = '/meal-plans';
                }
            });
        });

        // Search functionality
        const searchInput = document.querySelector('input[type="text"]');
        searchInput.addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                const query = this.value.trim();
                if (query) {
                    // In a real app, you would redirect to search results
                    alert(`Searching for: ${query}\n\nThis would redirect to search results in a real application.`);
                    this.value = '';
                }
            }
        });

        // Add floating animation to more elements
        const floatingElements = document.querySelectorAll('.floating');
        floatingElements.forEach((element, index) => {
            element.style.animationDelay = `${index * 0.2}s`;
        });
    });
</script>
</body>
</html>